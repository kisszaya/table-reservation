import { TableModel } from "./table.model";
import { TableTagModel } from "./table-tag.model";
import { TagModel } from "./tag.model";
import { SeatModel } from "./seat.model";

const models = [TagModel, TableModel, TableTagModel, SeatModel];

export { models, TagModel, TableModel, TableTagModel, SeatModel };
