import { IEditableCardWithInputsProps } from "../../types";

export const EditableCardWithInputs = (props: IEditableCardWithInputsProps) => {
  const { editable, fields } = props;
  return <div>EditableCardWithInputs</div>;
};
