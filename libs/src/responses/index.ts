
export namespace Responses {
  export interface UserLogin {
    accessToken: string;
    refreshToken: string;
  }

  export interface UserRegister {
    accessToken: string;
    refreshToken: string;
  }
}
