import { createStyles } from "@mantine/core";

export const useStyles = createStyles((theme) => ({
  container: {
    width: "49%",
    textDecoration: "solid !important",
  },

  card: {},

  icon: {
    color: theme.colors.gray[5],
  },

  noTextDecoration: {

  },
}));
